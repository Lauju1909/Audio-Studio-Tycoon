import sys

with open('competitor_ai.py', 'r', encoding='utf-8') as f:
    content = f.read()

# Make sure we add quality boost and console logic to both Basic and Advanced AIs!
# Actually we can just apply it to the returned RivalGame right after process in logic.py!
